// The cutter a holder cuts out of itself to wrap a 16" MacBook Pro: the
// device's published size grown by a millimetre all round, its corner and
// edge radii grown with it. The size is the assertion; the radii are the
// table's, read off photographs and labelled so there.
return device("macbook-pro-16", { clearance: 1 });
