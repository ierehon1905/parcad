[package]
name = "parcad-evaluation"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "What one evaluation of a parcad part is: the snapshot every transport serialises"

[dependencies]
parcad-core = { path = "../parcad-core" }
# Default features: the protocol types only, no OpenCASCADE.
parcad-occt = { path = "../parcad-occt" }
serde.workspace = true
serde_json.workspace = true
schemars = "1.2.2"
